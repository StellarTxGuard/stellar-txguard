{
  "name": "stellar-txguard",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic safety engine for evaluating whether a Stellar transaction is safe to retry.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
